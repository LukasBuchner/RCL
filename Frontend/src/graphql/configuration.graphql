query GetSchedulingConfiguration {
    schedulingConfiguration {
        timeToPixelScale
        baseYOffset
        siblingSpacing
        containerTopPadding
        containerBottomPadding
        baseHeight
        routerDropdownHeight
    }
}
